# Package configuration
package-lock=true
save-exact=false

# Publishing configuration
# Enable provenance for supply chain security (requires npm 9.5.0+)
# This generates build provenance that helps users verify package authenticity
provenance=true

# Access level (change to 'public' when ready to publish)
# access=public
